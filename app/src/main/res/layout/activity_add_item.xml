<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".AdditemActivity">

    <EditText
        android:id="@+id/name"
        android:textColor="@color/colorPrimary"
        android:textSize="@dimen/add_item_editText"
        android:layout_marginTop="100dp"
        android:hint="@string/add_item_hint_name"
        android:layout_marginStart="32dp"
        android:layout_marginEnd="32dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />
    <EditText
        android:id="@+id/price"
        android:textColor="@color/colorPrimary"
        android:textSize="@dimen/add_item_editText"
        android:layout_marginTop="16dp"
        android:hint="@string/add_item_hint_price"
        android:inputType="number"
        android:layout_marginStart="32dp"
        android:layout_marginEnd="32dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <Button
        android:id="@+id/add_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:layout_marginTop="48dp"
        android:background="@null"
        android:drawableStart="@drawable/ic_check"
        android:drawablePadding="8dp"
        android:enabled="false"
        android:textColor="@color/color_btn_add"
        android:text="@string/add_item_button_add"
        android:textSize="@dimen/add_item_button"
         />

</LinearLayout>